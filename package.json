{
  "name": "vue-dirty-guard-monorepo",
  "private": true,
  "scripts": {
    "dev": "pnpm -C playground dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "@types/node": "^25.0.9",
    "@vitest/coverage-v8": "1.6.1"
  }
}